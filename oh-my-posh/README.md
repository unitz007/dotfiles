# oh-my-posh (placeholder)

This directory is a placeholder for the **oh-my-posh** module.  
Replace its contents with the official `oh-my-posh` module files, or let the installer copy it from another source.

The installer (`install.ps1`) will copy the contents of this directory to:

```
$HOME\Documents\PowerShell\Modules\oh-my-posh
```

After installation, the PowerShell profile will automatically import the module and set a default theme.