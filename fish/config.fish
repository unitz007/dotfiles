# Minimal Fish configuration

# Add local bin to PATH
set -gx PATH $HOME/.local/bin $PATH

# If Fisher is installed, you can manage plugins here.
# Example: install a plugin (uncomment to use)
# if type -q fisher
#     fisher install some/plugin
# end