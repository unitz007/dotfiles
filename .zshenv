# .zshenv – environment variables for Zsh
# This file is sourced for every Zsh session (login and non‑login).

# Add personal bin directory to PATH
export PATH="$HOME/bin:$PATH"

# Preferred editor
export EDITOR=vim